---
interface Props {
  src: string;
  alt?: string;
}
const { src, alt }: Props = Astro.props;
---

<img
  src={src}
  alt={alt}
  class="w-full h-64 rounded-lg md:w-2/3 lg:w-1/3 object-cover object-center"
/>
