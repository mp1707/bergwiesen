---
interface Props {
  addStyles?: string;
}
const { addStyles }: Props = Astro.props;
---

<article
  class={`w-full md:w-2/3 flex flex-col lg:flex-row items-center lg:items-start gap-5 text-sm justify-center ${addStyles}`}
>
  <slot />
</article>
